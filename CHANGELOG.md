# Changelog

## v3.1.0 — 2026-07-18

补同步 AGENTS 模板（v3.0.0 遗漏——未重读并行会话改动的项目文档）：

- 新增「场景上下文」节：PRODUCT/DESIGN 按需引入接线（条目+引入时机为通用基线，可续列项目场景文档）；
- 「编码」→「编码开发」、「开发」→「语言」标题重命名；
- 「约束」扁平化为单列表（去输入/输出分组），新增「注意嵌入代码位置上下文逻辑」条，「噪音注释」→「噪音内容」；
- 指南首句收敛为「倾向谨慎而非速度。」；
- 删除「认知偏差记录」节（项目实践已放弃该机制，经用户裁决跟随删除）。

## v3.0.0 — 2026-07-18

完整同步（模板按项目文档整体重构，方向仍为 项目 → 模板）：

- PRODUCT 模板重构：弃用「模块清单表格 + 四节」结构，改为镜像用户结构偏好——业务分组（云/业务线）→ 应用小节（位置/职责/状态 要点 + 模块→子模块 + 增删改查关联/特殊字段子标题）→ 平台层 → 模块关联 → 业务顺序 → 应用关联图；应用=标题+要点不用表格；状态取值枚举（在建｜稳定｜存量｜空壳｜停用）入模板。
- TODO 模板重构：弃用「场景→功能→点」单树，改为镜像项目结构——状态约定/全局排除/应用清单来源 头部 + 「一、公共待办」（主题树，保留任务规格子列表）+ 「二、应用待办」（分组→应用→模块→子模块→点，骨架与 PRODUCT 同源）+ 「历史归档」（保留）。
- AGENTS / DESIGN / PROMPT 模板：项目侧无新结构，无变化。

破坏性说明：由 v2 PRODUCT/TODO 模板生成的文档与新模板结构不兼容，重新对齐请走「更新」。

## v2.2.0 — 2026-07-18

首次执行「同步」，自 js-console-platform 项目文档反哺四处结构：

- TODO 模板：恢复「历史归档」节（日期流水待办整段保留规则，v1.1 曾有、v2 重建时丢失）；示例补「任务规格子列表」格式（目标/匹配文件/约束）；
- AGENTS 模板：新增「操作手册」节挖空点（项目专项操作文档指引）；
- DESIGN 模板：新增「§8 规范覆盖的组件清单」，原落地说明顺延为 §9。

## v2.1 — 2026-07-18

按 writing-great-skills 审查修剪：

- description 塌缩为三个真实触发分支（新项目 / 旧项目接入 / 点名操作任一文档），删去同义重复与无效触发；
- 模板机制引入「填空/挖空」引导词：初始化/更新 = 填空，同步 = 挖空；
- 「更新」操作补可穷举的完成判据：逐章节四选一处置（补齐/修正/保留/无变化），全部章节有交代才算完成；
- 「不重排/不改写」等禁止式改为正向表述；「不编造」双写收敛为单一出处（填空规则）；
- 操作选择规则从「管辖范围」归位到「执行纪律」。

## v2.0 — 2026-07-18

按规格化需求重建：

- SKILL.md：初始化 / 更新 / 同步 三操作全部改为 BDD（Gherkin 场景 + 步骤/规则）；明确硬约束「仅管辖五份同名根文档」。
- templates/PRODUCT.md 新增（模块清单 / 模块关联 / 业务顺序 / 增删改查关联 / 关系图）。
- templates/PROMPT.md 条目改为 Given/When/Then 段式。
- 样例项目冒烟测试（子代理独立执行初始化）通过，并修复空态/降级分支：
  - 开发命令按配置声明列出，不要求实测；
  - DESIGN 无设计源时降级为「以 UI 库默认主题为唯一基准 + 整节精简为待补充」；
  - 无内容可填时空表头等悬空结构一并删除；
  - TODO 来源补「仓库现状勘察」，无可提取内容时保留示例作格式示范；
  - AGENTS frontmatter 仅在有消费工具（如 Cursor rules）时保留；
  - 「禁止依赖方向」等规则性结论无代码依据时一律待补充，勿推定。

## v1.1 — 2026-07-17

- 初版：SKILL.md（初始化/更新/同步）+ 五模板。
- 首次模板反哺：AGENTS 补 agent 临时产物目录与认知偏差记录；DESIGN 补响应式断点；PROMPT 补方案生成/提交前审查/修复复用；TODO 补流水式待办归档规则。
